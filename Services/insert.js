/*const customers = require('../Database/schema');
function insert(fname,lname,emailid,addr,city,state)
{
    return new Promise(rej,resove)=>const customer = new customers({
            firstname : fname,
            lastname :  lname,
            email:emailid,
            address : addr,
            city : city,
            state : state
    });
    customer.save();
}*/